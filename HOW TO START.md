# RailTransit 入门教程

本教程带你从零开始，在 Paper 26.2 + Slimefun4 服务器上搭建并运营第一条轨道交通线路。

---

**准备工作**

- 服务器已安装 Slimefun4 插件
- 已将 `RailTransit v1.0.2.jar` 放入 `plugins` 目录并重启服务器
- 拥有 `railtransit.use` 权限

**第 1 步：合成核心物品**

在增强工作台中合成两种物品：

- **列车**：配方为动力矿车，成品是"列车"（矿车外观）
- **指挥台**：配方需要铜线、红石块、基础电路板等，成品是"指挥台"（讲台外观）

**第 2 步：放置指挥台并成立组织**

1. 在地面放置指挥台
2. 不潜行右键指挥台，打开轨道交通控制台
3. 点击"成立组织"，按提示设置组织名称与介绍
4. 邀请队友加入组织（管理组织界面操作）

**第 3 步：铺设轨道**

使用原版铁轨铺设线路，注意四种铁轨的分工：

- **普通铁轨**：普通路段
- **高速铁轨**（动力铁轨）：高速路段
- **站停铁轨**（激活铁轨）：铺在每一个站台位置
- **侧线铁轨**（探测铁轨）：铺在线路起点（回车厂）

建议顺序：先在起点铺设一段侧线铁轨，然后沿规划路线铺设普通/高速铁轨，在每个站点位置放置站停铁轨。如果线路需要回车厂，终点也铺设侧线铁轨；如果是环线，终点直接接回第一站。

**第 4 步：添加站台**

1. 右键指挥台 → "规划铁路" → "添加站台"
2. 关闭界面后，空手右键一个站停铁轨（激活铁轨）
3. 在聊天框输入站台名称（输入 `cancel` 可取消）

重复以上步骤添加全部站台。站台会保存到全局站台列表，可被多条线路复用。

**第 5 步：创建线路并绑定站台**

1. 右键指挥台 → "规划铁路" → "编辑线路"
2. 选择你的组织，点击"添加线路"，按提示输入线路名称
3. 点击"编辑已有线路"进入线路列表，选择刚创建的线路
4. 点击"编辑经过站台"，从世界站台列表中添加该线路要经过的站台（按运行顺序添加）

**第 6 步：设置运行模式**

进入线路的"编译界面"：

- **是否回厂**：点击切换
  - 开启 = 单行线：侧线 → 站台按序 → 返回侧线
  - 关闭 = 环线：侧线 → 站台按序 → 回到第一站
- **编译结果**：显示当前线路的编译状态与运营参数

也可以用指令设置运营参数：

- `/railtransfer out [组织id] [线路id]`：回车厂后再次发车时间（秒）
- `/railtransfer wait [组织id] [线路id]`：每站停留时间（秒）

**第 7 步：编译线路**

编译是让列车知道"怎么走"的关键步骤：

1. 在编译界面点击"重新编译"，或在聊天框输入 `/railtransfer compile [组织id] [线路id]`
2. 空手右键起点侧线铁轨（探测铁轨）
3. 插件会自动沿铁轨 DFS 寻路，途经全部目标站台
4. 编译成功后提示"编译成功！该线路已可正常寻路运行"

**小技巧：断点续编**

- 如果编译途中遇到未加载区块，插件会提示你移动到该位置附近
- 到达后输入 `/railtransfer continue`（不带参数会用你最近编译的线路）即可继续编译
- 也可以使用 `/railtransfer continue [组织id] [线路id]` 指定线路续编
- 不需要跑回起点侧线

**第 8 步：发车运营**

1. 输入 `/railtransfer select [组织id] [线路id]` 选中线路
2. 手持"列车"（RT_TRAIN），右键线路路径上的侧线铁轨
3. 列车会自动接入线路，沿编译路径循环运行：
   - 到站自动报站"到达站点：xxx"，停留 `wait` 秒
   - 完成一圈回到起点后等待 `out` 秒再次发车
   - 环线模式下也可在路径关键点处发车

**第 9 步：日常维护**

- **修改运营参数**：使用 `out` / `wait` 指令调整发车间隔和站停时间
- **调整站台**：在线路"站台编辑"中增删站台，修改后必须重新编译
- **重新编译**：任何轨道走向或站台变更后，重新进入编译流程
- **服务器重启**：插件会自动恢复重启前列车的运行，无需手动重新发车

---

**常见问题**

**Q：编译提示"请空手右键一个侧线铁轨"？**
A：必须空手（主手为空）右键探测铁轨（DETECTOR_RAIL），手持任何物品都会提示失败。

**Q：编译时提示遇到未加载区块？**
A：这是正常机制。移动到提示位置附近加载区块后，输入 `/railtransfer continue` 继续编译。

**Q：列车放上去没有反应？**
A：检查三点：是否已用 `select` 选中线路、该线路是否编译成功、放置位置是否在编译路径的关键点上。

**Q：修改站台后列车还在跑旧路线？**
A：线路站台变更后编译结果会失效，需要重新编译，并重新放置列车。

**Q：列车停住不动了？**
A：插件有卡死自动纠正机制，正常情况下会自行恢复；若长时间卡死在同一位置，可能是转角过大。将你的铁轨薅直。
